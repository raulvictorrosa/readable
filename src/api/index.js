import { fetchCategories } from './categories';
import { /* fetchPostsByCategory, */ fetchPosts, addPost, /* fetchPostById, */ upvotePost, downvotePost, editPost, deletePost } from './posts';
import { /* fetchComments, */ addComment, fetchComment, upvoteComment, downvoteComment, editComment, deleteComment } from './comments';

export default {
  fetchCategories,
  // fetchPostsByCategory,
  fetchPosts,
  addPost,
  // fetchPostById,
  upvotePost,
  downvotePost,
  editPost,
  deletePost,
  // fetchComments,
  addComment,
  fetchComment,
  upvoteComment,
  downvoteComment,
  editComment,
  deleteComment
}
